import type { Body } from '../core/TilemapLoader';

export function aabb(a: Body, b: Body): boolean {
  return (
    a.x < b.x + b.width &&
    a.x + a.width > b.x &&
    a.y < b.y + b.height &&
    a.y + a.height > b.y
  );
}
